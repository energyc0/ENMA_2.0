class Dog{
    field name;

    meth sound(){
        println("Bark!");
    }

    meth eat(food){
        println(name, " has eaten ", food, ". Yammy!");    
    }

    meth getname(){
        return name;
    }
}

func main(){
    var bro = Dog();
    bro.name = "Oliver";
    println("Dog's name is ", bro.getname());
    bro.sound();
    bro.eat("sandwich");
}